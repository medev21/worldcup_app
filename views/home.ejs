<!DOCTYPE html>
<html lang="en">
  <head>
    <% include partials/header.ejs %>
  </head>
  <body>
    <h1>Hello, world!</h1>

    <% for(var i=0; i < worldcups.length; i++){ %>
      <img src="images/<%= worldcups[i].poster %>" alt="" class="img-responsive"/>
    <% } %>

    <% include partials/script.ejs %>
  </body>
</html>
